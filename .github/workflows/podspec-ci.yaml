name: CI

on:
  push:
    branches:
      feat/cocoapods-support

jobs:
  build-and-test:
    runs-on: macOS-latest
    steps:
      - uses: actions/checkout@v2

      - name: Lint native library
        run: |
          cd Native
          pod lib lint --verbose --allow-warnings
